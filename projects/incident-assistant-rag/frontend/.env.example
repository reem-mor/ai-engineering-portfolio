# IncidentIQ frontend — copy to .env only if the API is NOT on http://localhost:8000.
# The frontend has no secrets; it only needs the backend API base URL.
VITE_API_BASE_URL=http://localhost:8000/api
