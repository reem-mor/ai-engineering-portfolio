# --- AWS Bedrock ---
# Region where your Knowledge Base lives (must match KB region).
AWS_REGION=us-east-1

# The Knowledge Base ID copied from the Bedrock console.
BEDROCK_KB_ID=XXXXXXXXXX

# Foundation-model or inference-profile ARN used for answer generation.
# Legacy Claude 3 Haiku IDs may be blocked — prefer an inference profile, e.g.:
#   arn:aws:bedrock:us-east-1:<account-id>:inference-profile/us.amazon.nova-lite-v1:0
#   arn:aws:bedrock:us-east-1:<account-id>:inference-profile/us.anthropic.claude-haiku-4-5-20251001-v1:0
BEDROCK_MODEL_ARN=arn:aws:bedrock:us-east-1::foundation-model/anthropic.claude-3-haiku-20240307-v1:0

# How many chunks the KB retrieves per query (3-10 is sane).
BEDROCK_NUM_RESULTS=5

# --- Flask ---
# Long random string. Generate with:  python -c "import secrets; print(secrets.token_hex(32))"
FLASK_SECRET_KEY=replace-me-with-a-long-random-hex-string

# Set to "production" on EC2, "development" locally.
FLASK_ENV=development

# NOTE: no AWS_ACCESS_KEY_ID / AWS_SECRET_ACCESS_KEY here on purpose.
# On EC2 we rely on the IAM instance profile.
# Locally, use `aws configure` or AWS_PROFILE.
