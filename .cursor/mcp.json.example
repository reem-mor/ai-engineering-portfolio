{
  "_comment": "Optional local MCP overrides (gitignored). Default server list: ../.mcp.json (includes playwright, kaggle, aws-api, course-tools). Copy blocks here only to override — never hardcode API keys.",
  "mcpServers": {}
}
