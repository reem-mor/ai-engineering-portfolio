# Course-wide environment template (placeholders only — never commit real keys)
# Copy to .env and fill in locally: copy .env.example .env

# OpenAI — RAG homework and capstone (capstone also uses backend/.env)
OPENAI_API_KEY=your_openai_api_key_here
OPENAI_MODEL=gpt-4o-mini
EMBEDDING_MODEL=text-embedding-3-small

# Google AI — lecture 06 Flask RAG demo
GEMINI_API_KEY=your_gemini_api_key_here

# Hugging Face — lecture NLP demos
HF_TOKEN=your_huggingface_token_here

# Flask
FLASK_ENV=development

# IncidentIQ capstone uses its own backend/.env — see projects/incident-assistant-rag/README.md
