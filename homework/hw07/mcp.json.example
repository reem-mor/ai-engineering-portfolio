# Optional MCP overrides for hw07 — canonical servers are in repo-root `.mcp.json`:
#   kaggle (HTTP), openwebui (stdio), rapidapi (stdio), playwright (stdio)
# Requires in .env: KAGGLE_API_TOKEN, OWUI_URL, OWUI_API_KEY, RAPIDAPI_KEY, RAPIDAPI_HOST
# Never hardcode API keys here.

{
  "mcpServers": {}
}
