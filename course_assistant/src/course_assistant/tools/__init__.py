"""Agent tools.

The MVP exposes a small, well-defined tool set, added phase by phase:

- ``drive_lookup`` — homework files, recording links, instructor code links (Phase 2)
- ``search_course_materials`` — RAG retrieval over Drive materials (Phase 3)
- ``explain_homework_submission`` — the canonical submission procedure (Phase 4)
- homework email compose / preview / send-after-approval (Phase 4)
"""
