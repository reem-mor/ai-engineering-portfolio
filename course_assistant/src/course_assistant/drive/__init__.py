"""Read-only Google Drive access to the course folder (Phase 2).

Provides the service that lists homework files, recordings, and instructor code
links behind an interface so it can be faked in tests (no live calls in CI).
"""
