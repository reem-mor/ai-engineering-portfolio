# Development tooling (CI + local lint/test helpers)
# Install: pip install -r requirements-dev.txt
# Course ML stack lives in requirements.txt — do not merge blindly.

ruff>=0.15.20
pytest>=8.0.0
pytest-asyncio>=0.24.0
