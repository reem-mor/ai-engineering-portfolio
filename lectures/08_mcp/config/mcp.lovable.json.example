{
  "_comment": "Optional remote MCP — copy only the lovable block into your existing .cursor/mcp.json mcpServers (keep course-tools, playwright, AWS, etc.). Docs: https://docs.lovable.dev/integrations/lovable-mcp-server",
  "mcpServers": {
    "lovable": {
      "type": "http",
      "url": "https://mcp.lovable.dev",
      "auth": {
        "CLIENT_ID": "${env:LOVABLE_CLIENT_ID}"
      }
    }
  }
}
